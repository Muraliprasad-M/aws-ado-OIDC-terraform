# azure-pipelines.yml
trigger: none

pool:
  name: Default        # <-- your self-hosted pool name (no vmImage here)

steps:
- script: |
    echo "Hello from self-hosted agent: $(Agent.Name)"
    uname -a
    which git && git --version
  displayName: "Verify agent is alive"
