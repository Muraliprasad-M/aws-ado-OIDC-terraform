trigger:
- main

pool:
  #vmImage: ubuntu-latest
  name: LTIN451676

steps:
- checkout: self

- task: AWSCLI@1
  inputs:
    awsCredentials: 'aws-oidc'
    regionName: 'us-west-2'
    awsCommand: 'sts'
    awsSubCommand: 'get-caller-identity'
